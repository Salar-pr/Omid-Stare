# گزارش ممیزی امنیتی — Omid-Stare

**تاریخ:** ۱۴ شهریور ۱۴۰۴ (2026-09-04)
**دامنه:** احراز هویت + امنیت کلی سایت
**روش:** بازبینی ایستای کد سرور + حدود ۶۰ تست جعبه‌سیاه زنده روی `localhost:3000`
**شاخه:** `arena/01a06651-omid-stare` — کامیت اصلاحات: `ed0ddab`

---

## خلاصهٔ مدیریتی

معماری احراز هویت این پروژه **به‌طور غیرمنتظره‌ای خوب** است. تقریباً همهٔ کلاسیک‌های OWASP را تست کردم و رد شدند: تزریق SQL، دور زدن مجوز، بالا بردن سطح دسترسی، دستکاری قیمت، IDOR، شمارش کاربر، نشت فایل‌های حساس. تصمیمات پایه‌ای درست گرفته شده — توکن سشن مات ۲۵۶ بیتی، فقط sha256 در دیتابیس، Argon2id، و hash ساختگی هنگام لاگین برای جلوگیری از نشت زمانی.

**۴ مورد پیدا شد و هر ۴ اصلاح و تست شدند.** هیچ آسیب‌پذیری بحرانی (دسترسی مستقیم مهاجم به داده یا حساب) وجود نداشت.

| # | یافته | شدت | وضعیت |
|---|---|---|---|
| ۱ | Rate limit به‌جای ۴۲۹ کد ۵۰۰ برمی‌گرداند | متوسط | ✅ اصلاح شد |
| ۲ | `COOKIE_SECRET` پیش‌فرضِ هاردکد در production | متوسط | ✅ اصلاح شد |
| ۳ | `trustProxy: true` بی‌قیدوشرط → جعل IP | متوسط | ✅ اصلاح شد |
| ۴ | هیچ دفاع CSRF جز کوکی SameSite | متوسط | ✅ اصلاح شد |
| ۵ | نبودِ CSP | پایین | ⬜ باز (توضیح در پایین) |
| ۶ | `innerHTML` خام برای محتوای CMS | پایین | ⬜ باز (منبع فقط ادمین) |

---

## یافته‌های اصلاح‌شده

### ۱. محدودکنندهٔ نرخ کد ۵۰۰ می‌داد به‌جای ۴۲۹ — متوسط

**چه بود:** ۱۳ لاگین ناموفق پشت‌سرهم زدم. نتیجه:

```
401 401 401 401 401 500 500 500 500 500 500 500 500
```

از درخواست ششم به بعد سرور واقعاً جلوی درخواست را می‌گرفت (یعنی محافظت brute-force **کار می‌کرد**)، ولی خطا را به‌عنوان `unhandled error` لاگ می‌کرد و ۵۰۰ برمی‌گرداند. در لاگ سرور مستقیماً دیده می‌شد:

```
"level":50, "err":{"success":false,"error":{"code":"RATE_LIMITED",...}}, "msg":"unhandled error"
```

**چرا مهم بود:** سه پیامد داشت. اول، کاربری که رمزش را اشتباه زده پیام «خطای داخلی سرور» می‌گیرد نه «زیاد تلاش کردی، چند دقیقه صبر کن» — تجربهٔ گمراه‌کننده. دوم، هیچ اطلاعاتی از زمان انتظار به کلاینت نمی‌رسید. سوم و مهم‌تر برای عملیات: ۵۰۰ها داشبورد مانیتورینگ را پر می‌کنند، پس یک حملهٔ brute-force واقعی داخل نویز خطاهای معمولی گم می‌شود.

**ریشه:** `errorResponseBuilder` در `app.js` فقط بدنهٔ JSON را برمی‌گرداند بدون `statusCode`. وقتی این شیء throw می‌شد، `fail()` در `utils/respond.js` دنبال `err.statusCode === 429` یا `err.code === 'FST_RATE_LIMIT'` می‌گشت — هیچ‌کدام روی شیء نبود — و به شاخهٔ عمومی ۵۰۰ می‌افتاد.

**اصلاح:** هم `statusCode: 429` و `code: 'FST_RATE_LIMIT'` به سازندهٔ پاسخ اضافه شد و هم `retryAfterSec` از `context.ttl`. ضمناً `fail()` را مقاوم کردم تا اگر شکل خطا در نسخه‌های بعدی کتابخانه عوض شد، با تشخیص `err.error.code === 'RATE_LIMITED'` باز هم درست جواب دهد.

**تأیید پس از اصلاح:**
```
401 401 401 401 401 401 401 401 401 401 429 429 429
{"success":false,"error":{"code":"RATE_LIMITED","message":"...","retryAfterSec":900}}
```

---

### ۲. راز کوکیِ هاردکد در production — متوسط

**چه بود:** در `config/index.js`:

```js
cookieSecret: process.env.COOKIE_SECRET || (isProd ? 'omid-stare-cookie-signing' : undefined),
```

منطق برعکس شده بود: در حالت توسعه اگر متغیر نبود `undefined` می‌شد (که خطا می‌داد و توجه را جلب می‌کرد)، ولی در **production** بی‌سروصدا به یک رشتهٔ ثابت که داخل مخزن گیت است برمی‌گشت.

**چرا مهم بود:** این رشته در سورس عمومی است. هر کسی که مخزن را ببیند می‌تواند کوکی امضا کند. اگر یک بار با `COOKIE_SECRET` تنظیم‌نشده دیپلوی می‌شد، سایت بدون هیچ هشداری بالا می‌آمد و راز امضا عملاً عمومی بود.

**اصلاح:** بازگشت پیش‌فرض حذف شد. به‌جایش یک بررسی fail-fast هنگام بالا آمدن اضافه شد که فقط در production فعال است و اگر `COOKIE_SECRET` نبود یا کوتاه‌تر از ۳۲ کاراکتر بود، رمز پیش‌فرض دیتابیس (`omid:omid`) هنوز سرجایش بود، `COOKIE_SECURE` خاموش بود، یا `ADMIN_PASSWORD` هنوز نمونه بود — سرور با پیام فارسی و دستور ساخت secret بالا نمی‌آید. توقف راه‌اندازی خیلی بهتر از رخنهٔ خاموش است.

---

### ۳. `trustProxy: true` بی‌قیدوشرط — متوسط

**چه بود:** `app.js` بدون شرط به هدر `X-Forwarded-For` اعتماد می‌کرد.

**چرا مهم بود:** این مورد **یافتهٔ ۱ را تشدید می‌کرد**. محدودیت نرخ بر پایهٔ IP است و IP از `X-Forwarded-For` خوانده می‌شود. با `trustProxy: true` و بدون پراکسی جلوی اپ، مهاجم فقط کافی است هر درخواست را با یک `X-Forwarded-For` تصادفی بفرستد تا سهمیهٔ ۱۰ تلاش در ۱۵ دقیقه عملاً بی‌نهایت شود — یعنی محافظت brute-force کاملاً دور زده می‌شد. ضمناً IP ثبت‌شده در رکورد سشن‌ها هم قابل جعل بود، که ردیابی حادثه را بی‌اعتبار می‌کند.

**اصلاح:** به متغیر `TRUST_PROXY` وصل شد با پیش‌فرض **امن** (`false`). مقادیر پشتیبانی‌شده: عدد (تعداد هاپ)، لیست IP/CIDR، یا `true`. در `.env.example` مستند شد.

> نکته برای دیپلوی: اگر سایت پشت nginx یا Cloudflare می‌رود، **باید** `TRUST_PROXY=1` ست شود، وگرنه همهٔ کاربران از دید rate limiter یک IP واحد (خودِ پراکسی) خواهند بود.

---

### ۴. نبودِ دفاع CSRF مستقل — متوسط

**چه بود:** احراز هویت کوکی‌محور است و تنها دفاع موجود `SameSite` بود (`Lax` در توسعه، `Strict` در production). هیچ توکن CSRF و هیچ بررسی Origin وجود نداشت. تست زنده: `POST /api/cart/items` با هدر `Origin: https://evil.example.com` → **۲۰۰**.

**چرا مهم بود:** `SameSite=Strict` دفاع خوبی است ولی تنها لایه بودنش شکننده است — مرورگرهای قدیمی نادیده‌اش می‌گیرند، و سناریوهای same-site (مثل یک زیردامنهٔ آسیب‌دیده) از آن عبور می‌کنند. تکیه بر یک کنترل که کاملاً در دست مرورگر است، برای عملیات تغییردهنده کافی نیست.

**اصلاح:** میان‌افزار `middlewares/csrf.js` اضافه شد و به‌صورت hook روی کل `/api` نشست:
- فقط روی `POST/PUT/PATCH/DELETE` اعمال می‌شود؛ `GET/HEAD/OPTIONS` آزادند.
- `Origin` (و در نبودش `Referer`) با `APP_URL` + میزبان خود درخواست + `CSRF_ALLOWED_ORIGINS` مقایسه می‌شود.
- اگر **هیچ‌کدام** از این دو هدر نبود، رد نمی‌کند — چون CSRF ذاتاً حملهٔ مبتنی بر مرورگر است و مرورگر همیشه `Origin` می‌فرستد. این کار کلاینت‌های موبایل و اسکریپت‌ها را نمی‌شکند.

**تأیید پس از اصلاح:**
```
Origin بیگانه (evil.example.com) → 403 CSRF_BLOCKED
Origin درست (localhost:3000)     → 401 (یعنی از CSRF رد شد، به authz رسید) ✓
بدون Origin (curl)               → 401 ✓
صفحات عادی /، /albums.html، /api/* → همه ۲۰۰ ✓
```

---

## موارد باز (آگاهانه اصلاح نشده)

### ۵. نبودِ Content-Security-Policy — پایین
helmet فعال است ولی CSP عمداً خاموش شده چون صفحات اسکریپت و استایل inline دارند. افزودن CSP الان یعنی یا `unsafe-inline` (که بی‌فایده‌اش می‌کند) یا بازنویسی همهٔ صفحات. چون هیچ مسیر XSS واقعی پیدا نشد، این را به‌عنوان بدهی فنی می‌گذارم نه آسیب‌پذیری فعال. مسیر درست: انتقال inlineها به فایل جدا، بعد CSP سخت‌گیرانه.

### ۶. `innerHTML` خام برای محتوای CMS — پایین
در `js/home.js` خطوط ۴۴، ۴۶ و ۵۲ محتوای CMS (`manifest.quote`، `body`، شبکه‌های اجتماعی) مستقیم داخل `innerHTML` می‌رود. منبع این داده فقط `PUT /api/admin/content/:key` است، یعنی مهاجم باید از قبل ادمین باشد. ریسک واقعی پایین است، ولی «XSS ذخیره‌شده توسط ادمین» یعنی یک ادمین آسیب‌دیده می‌تواند به همهٔ بازدیدکنندگان اسکریپت بدهد. اگر خواستی، همان `escHtml` موجود را اینجا هم اعمال کنم.

---

## چیزهایی که تست شدند و **رد شدند** (سالم)

**مجوزدهی —** همهٔ مسیرهای محافظت‌شده بدون احراز هویت ۴۰۱ دادند: `/api/admin/users`, `/api/admin/dashboard`, `/api/admin/orders`, `/api/cart`, `/api/orders`, `/api/users/me`, `/api/wishlist`.

**بالا بردن سطح دسترسی —** کاربر عادی ساختم و تلاش کردم خودم را ادمین کنم. `PATCH /api/admin/users/:me {role:admin}` → ۴۰۳. `PATCH /api/users/me {role:admin}` → ۴۰۰. نقش تغییر نکرد. سرویس ادمین با zod فیلدهای `role`/`isActive` را whitelist می‌کند و از تنزل خودِ ادمین جلوگیری می‌کند.

**دستکاری قیمت —** `POST /api/cart/items` با `price:1` و `unitPrice:1` تزریقی → سرور قیمت واقعی ۹۸۰٬۰۰۰ را ثبت کرد. تعداد `-5`، `0`، `99999`، `1.5` → همه ۴۰۰.

**IDOR —** کاربر B سعی کرد آیتم سبد کاربر A را حذف و ویرایش کند → هر دو ۴۰۴، سبد A دست‌نخورده.

**تزریق SQL —** `' OR '1'='1`، `'; DROP TABLE users;--`، `admin'--` روی لاگین → ۴۰۰. `?search=' OR 1=1--` → پاسخ عادی. جدول‌ها سالم. بازبینی کد: هر جا رشته درون کوئری درون‌ریزی می‌شود، مقدار از whitelist توسعه‌دهنده می‌آید (`ASC|DESC`، نام ستون‌ها) و بقیه پارامتری است.

**شمارش کاربر —** پیام خطای ایمیل موجود و ناموجود کاملاً یکسان. لاگین برای کاربر ناموجود هم یک hash ساختگی verify می‌کند تا اختلاف زمانی نشت نکند.

**سشن —** خروج، سشن را باطل می‌کند (۲۰۰ سپس ۴۰۱). کوکی `sid` جعلی → ۴۰۱. هیچ `password` یا `hash` در `/api/users/me` نیست. پاکسازی ساعتی سشن‌های منقضی وجود دارد.

**نشت فایل —** `/.env`, `/server/.env`, `/.git/config`, `/server/src/config/index.js`, `/docker-compose.yml`, `/server/package.json`, `/node_modules/x` → همه ۴۰۴. پیمایش مسیر با `../`, `..%2f`, `/media/../../.env` → ۴۰۴/۴۰۳.

**XSS ذخیره‌شده —** نام کاربر را `<script>alert(1)</script>` گذاشتم. API خام ذخیره می‌کند، ولی هر نقطهٔ رندر escape می‌کند: `escHtml` در `js/product.js` برای نظرات و پرسش‌ها، `textContent` در `js/nav.js` و `js/profile.js`. تنها sink خام مورد ۶ بالاست.

**نشت خطا —** UUID نامعتبر محصول → JSON تمیز `NOT_FOUND`، بدون stack trace.

---

## نقاط قوت طراحی

اینها را جدا می‌نویسم چون نشان می‌دهد پایهٔ کار درست چیده شده:

- توکن سشن مات ۲۵۶ بیتی، فقط sha256 آن ذخیره می‌شود — دیتابیس لو برود، سشن‌ها قابل بازسازی نیستند.
- Argon2id برای رمزها (نه bcrypt، نه چیز بدتر).
- verify با hash ساختگی برای کاربر ناموجود — جلوگیری فعال از نشت زمانی.
- پاکسازی ساعتی سشن‌های منقضی.
- اعتبارسنجی آپلود جدی: whitelist نوع MIME (jpeg/png/webp)، سقف حجم، نام فایل UUID تصادفی، پاک کردن آواتار قبلی.
- هدرهای امنیتی helmet: HSTS یک‌ساله با includeSubDomains، nosniff، X-Frame-Options، Referrer-Policy.
- در production کوکی `SameSite=Strict` + `Secure`.
- لاگ‌ها فیلدهای حساس (رمز، کد تخفیف، set-cookie) را REDACT می‌کنند.

---

## توصیه‌های بعدی (به ترتیب اولویت)

۱. هنگام دیپلوی حتماً `TRUST_PROXY` را متناسب با معماری واقعی ست کن — بررسی جدید راه‌اندازی جلوی خطاهای دیگر را می‌گیرد ولی این یکی را نمی‌تواند حدس بزند.
۲. `escHtml` را روی سه sink محتوای CMS در `js/home.js` اعمال کن.
۳. inlineهای اسکریپت/استایل را به فایل منتقل کن تا CSP سخت‌گیرانه ممکن شود.
۴. یک قفل حساب مبتنی بر ایمیل (نه فقط IP) اضافه کن — rate limit فعلی روی IP است و password spraying توزیع‌شده را نمی‌گیرد.
