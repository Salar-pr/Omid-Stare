# راهنمای اجرای پروژه روی کامپیوتر شخصی

سایت فروشگاهی **OMID RASTAR** — راهنمای کامل بالا آوردن پروژه از صفر.

---

## ۱. پیش‌نیازها

فقط یک چیز لازم دارید:

| نرم‌افزار | نسخه | لینک |
|---|---|---|
| **Node.js** | ۲۰ یا بالاتر | <https://nodejs.org> (نسخه LTS را بگیرید) |

**PostgreSQL لازم نیست نصب کنید!** پروژه یک نسخه‌ی جاسازی‌شده (embedded) از پستگرس دارد که خودش دانلود و اجرا می‌شود.

### بررسی نصب بودن Node

ترمینال (در ویندوز: PowerShell یا CMD) را باز کنید و بزنید:

```bash
node -v
```

اگر چیزی مثل `v20.11.0` یا بالاتر دید، آماده‌اید. اگر خطا داد یا عدد کمتر از ۲۰ بود، اول Node را نصب/به‌روز کنید.

---

## ۲. آماده‌سازی (فقط بار اول)

فایل زیپ را در یک پوشه باز کنید، بعد ترمینال را **داخل پوشه‌ی `server`** باز کنید.

> **راهنمای ویندوز:** داخل پوشه‌ی `server` بروید، در نوار آدرس بالای پنجره کلمه‌ی `cmd` را تایپ کنید و Enter بزنید. ترمینال دقیقاً همان‌جا باز می‌شود.

حالا این دستور را بزنید:

```bash
npm install
```

این کار چند دقیقه طول می‌کشد (یک بسته به اسم `argon2` باید کامپایل شود — صبور باشید).

<details>
<summary><b>اگر <code>npm install</code> در ویندوز خطای build داد، اینجا را باز کنید</b></summary>

بسته‌ی `argon2` برای کامپایل به ابزار build نیاز دارد. یکی از این دو راه:

**راه ۱ (ساده‌تر):** نصب ابزارهای build:
```bash
npm install --global windows-build-tools
```

**راه ۲:** نصب [Visual Studio Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/) با تیک زدن گزینه‌ی «Desktop development with C++».

</details>

---

## ۳. راه‌اندازی دیتابیس

**دو ترمینال جدا** لازم دارید. این ترمینال اول است.

در ترمینال **اول** (داخل پوشه‌ی `server`):

```bash
npm run db:embedded
```

منتظر بمانید تا این پیام‌ها را ببینید:

```
[db] PostgreSQL روی پورت 5432 بالا آمد ✅
[db] دیتابیس «omid_stare» ساخته شد ✅
[migrate] done. 3 total
[seed] product liquid-void-tshirt (stock=12)
[seed] album Liquid Void (4 tracks)
```

> ⚠️ **این ترمینال را نبندید.** تا وقتی باز است، دیتابیس روشن می‌ماند.

---

## ۴. اجرای سایت

یک ترمینال **دوم** باز کنید (باز هم داخل پوشه‌ی `server`) و بزنید:

```bash
npm start
```

باید ببینید:

```
Server listening at http://127.0.0.1:3000
db: up (18ms)
```

حالا مرورگر را باز کنید و بروید به:

### 🌐 <http://localhost:3000>

تمام! سایت بالا است.

---

## ۵. ورود به پنل مدیریت

آدرس: <http://localhost:3000/admin.html>

| | |
|---|---|
| **ایمیل** | `admin@omid-stare.local` |
| **رمز** | `ChangeMe_Strong_1405` |

در پنل ادمین می‌توانید محصولات، سفارش‌ها، کدهای تخفیف و **تب وضعیت ارسال** (سفارش‌های ارسال‌شده در برابر در انتظار) را ببینید.

برای کاربر عادی هم می‌توانید از صفحه‌ی <http://localhost:3000/account.html> ثبت‌نام کنید.

---

## ۶. دفعات بعد

بعد از بار اول، دیگر `npm install` لازم نیست. فقط دو ترمینال:

```bash
# ترمینال ۱
npm run db:embedded

# ترمینال ۲
npm start
```

---

## ۷. رفع مشکلات رایج

### «پورت ۳۰۰۰ در حال استفاده است» (EADDRINUSE)

یک برنامه‌ی دیگر پورت را گرفته. یا آن را ببندید، یا سایت را روی پورت دیگری اجرا کنید:

```bash
# ویندوز (PowerShell)
$env:PORT=4000; npm start

# مک / لینوکس
PORT=4000 npm start
```

بعد بروید به `http://localhost:4000`.

### همه‌جا خطای ۵۰۰ می‌دهد / محصولات نمایش داده نمی‌شوند / موقع ورود می‌گوید «خطای سرور»

این یعنی سایت بالا آمده ولی **به دیتابیس وصل نیست**. برای تشخیص دقیق، در پوشه‌ی `server` بزنید:

```bash
npm run doctor
```

این دستور همه چیز را چک می‌کند و دقیقاً می‌گوید مشکل کجاست. تقریباً همیشه علت این است که ترمینال دیتابیس اجرا نشده. مطمئن شوید **دو ترمینال** باز است:

```bash
# ترمینال ۱ — باید باز بماند
npm run db:embedded

# ترمینال ۲
npm start
```

در لاگ `npm start` اگر نوشته بود `db: down at boot` یعنی دیتابیس بالا نیست؛ وقتی درست باشد می‌نویسد `db: up`.

### خطای `شروع PostgreSQL ناموفق بود`

پوشه‌ی داده خراب شده. آن را پاک کنید تا از نو ساخته شود:

```bash
# ویندوز
rmdir /s /q .pgdata

# مک / لینوکس
rm -rf .pgdata
```

بعد دوباره `npm run db:embedded` را بزنید.

> ⚠️ توجه: این کار همه‌ی داده‌ها (حساب‌ها و سفارش‌ها) را پاک می‌کند و داده‌های اولیه را از نو می‌سازد.

### سایت بالا می‌آید ولی «خطای اتصال به دیتابیس» می‌دهد

یعنی ترمینال اول (دیتابیس) بسته شده یا هنوز کامل بالا نیامده. مطمئن شوید پیام `PostgreSQL روی پورت 5432 بالا آمد ✅` را دیده‌اید.

### پورت ۵۴۳۲ در حال استفاده است

اگر از قبل PostgreSQL روی سیستم دارید، با پستگرس جاسازی‌شده تداخل می‌کند. سرویس پستگرس خودتان را موقتاً متوقف کنید.

---

## ۸. اجرای تست‌ها (اختیاری)

```bash
npm test
```

۳۴ تست در ۶ فایل اجرا می‌شود. دیتابیس باید بالا باشد.

---

## ۹. ساختار پروژه

```
Omid-Stare/
├── *.html              ۱۳ صفحه‌ی سایت (index, shop, albums, account, ...)
├── css/                استایل‌ها (theme-psych.css = تم اصلی و رنگ‌ها)
├── js/                 اسکریپت‌های سمت کاربر (api.js = ارتباط با سرور)
├── images/             تصاویر
├── fonts/              فونت‌های محلی (Vazirmatn + Space Grotesk)
└── server/             بک‌اند Node.js + Fastify
    ├── src/
    │   ├── config/     تنظیمات و متغیرهای محیطی
    │   ├── db/         اتصال به دیتابیس
    │   ├── middlewares/ احراز هویت، CSRF، محدودیت نرخ
    │   └── modules/    منطق هر بخش (auth, products, orders, admin, ...)
    ├── migrations/     ساختار جداول دیتابیس
    ├── scripts/        ابزارها (migrate, seed, db-embedded)
    └── .env            تنظیمات محلی
```

---

## ۱۰. نکته‌ی مهم درباره‌ی امنیت

فایل `.env` برای **اجرای محلی** تنظیم شده است. اگر خواستید پروژه را روی سرور واقعی منتشر کنید، حتماً این‌ها را عوض کنید:

| متغیر | چرا |
|---|---|
| `NODE_ENV=production` | فعال کردن حالت امن (کوکی `SameSite=Strict`، محافظ CSRF کامل) |
| `COOKIE_SECRET` | یک رشته‌ی تصادفی و طولانی بگذارید |
| `ADMIN_PASSWORD` | رمز پیش‌فرض را حتماً عوض کنید |
| `DATABASE_URL` | آدرس دیتابیس واقعی |
| `APP_URL` | دامنه‌ی واقعی سایت |

در حالت `production` سرور اگر `COOKIE_SECRET` ضعیف باشد **عمداً بالا نمی‌آید** تا جلوی اشتباه را بگیرد.

---

## ۱۱. مستندات بیشتر

| فایل | محتوا |
|---|---|
| `PROJECT-OVERVIEW.md` | توضیح کامل و مفصل کل پروژه به فارسی |
| `SECURITY-AUDIT.md` | گزارش بررسی امنیتی و مواردی که اصلاح شد |
| `README.md` | مستندات اصلی مخزن |
